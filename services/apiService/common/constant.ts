// ============= 常量定义 =============
export const REQUEST_CONFIG = {
  DEFAULT_PAGE_SIZE: 100,
  MAX_PAGES: 100,
  HEADERS: {
    CONTENT_TYPE: "application/json",
    PRAGMA: "no-cache",
  },
} as const
